import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'lh3.googleusercontent.com', 'plus.unsplash.com', 'media.audubon.org'],
  },
};

export default nextConfig;
